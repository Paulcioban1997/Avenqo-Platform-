# ShipStation

- Readiness: `COMING_SOON`; no adapter is registered.
- Developer account: ShipStation account with API access under the applicable plan/version.
- Registration: create API credentials or an approved OAuth application when available for the chosen API version.
- Callback/webhook: none deployed; API-key authentication is currently planned in catalog metadata.
- Scopes: read orders, shipments/fulfillments, warehouses, carriers, and inventory where supported.
- Environment: future `SHIPSTATION_API_KEY`, `SHIPSTATION_API_SECRET`, `SHIPSTATION_API_BASE`.
- Sandbox/review: sandbox availability varies by API/program; use a dedicated test account and non-production orders.
- Acceptance: confirm API version and credential model, validate pagination, throttling, shipment updates, revocation, and the common gate.
